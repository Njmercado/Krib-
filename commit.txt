* Implementación de autocompletado:
  - Primero la conexion con el servidor para traer las palabras una vez se van presionando.
  - Implementación en el Front. El autocompletado se muestra en la tabla de resultados(espacio a la derecha).
* Cambio del fondo de pantalla de la pestaña curso, para que se vea un poco 'mejor'.
